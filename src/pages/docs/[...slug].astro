---
export const prerender = true;

const { slug } = Astro.params;
const slugPath = Array.isArray(slug) ? slug.join('/') : slug;
const target = slugPath ? `/atividades/${slugPath}` : '/atividades';

return Astro.redirect(target);
---
