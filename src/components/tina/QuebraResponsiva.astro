---
// Override de `break` pro <TinaMarkdown>: o editor insere essa quebra com
// Shift+Enter. Some no mobile (só quebra a linha em telas sm: pra cima),
// reproduzindo o <br class="hidden sm:inline" /> que existia no HTML cru.
---
<br class="hidden sm:inline" />
