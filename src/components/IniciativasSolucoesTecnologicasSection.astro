---
import { tinaField } from '@tinacms/astro/tina-field';

interface Props {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  data: any;
}

const { data } = Astro.props;
---

<section class="mx-auto max-w-4xl space-y-6 px-4 py-16">
  <h1
    class="text-primary text-3xl font-bold"
    data-tina-field={tinaField(data, 'titulo') || undefined}
  >
    {data?.titulo ?? 'Soluções Tecnológicas'}
  </h1>
  <p
    class="text-base-content"
    data-tina-field={tinaField(data, 'descricao') || undefined}
  >
    {
      data?.descricao ??
        'Conheça as ferramentas e a infraestrutura desenvolvidas pelo CPPS para apoiar as pesquisas em ciências humanas e sociais.'
    }
  </p>
</section>
