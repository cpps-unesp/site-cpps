---
interface Props {
  columns?: 1 | 2 | 3;
}

const { columns = 2 } = Astro.props;

const gridCols = {
  1: 'grid-cols-1',
  2: 'grid-cols-1 md:grid-cols-2',
  3: 'grid-cols-1 md:grid-cols-2 lg:grid-cols-3'
};
---

<div class={`grid gap-4 my-8 not-prose ${gridCols[columns]}`}>
  <slot />
</div>
